# Critic scope v1 的执行修复，不修改语义候选

原运行 `/raid/hfang/ihb/runs/v06212-critic-scope-v1` 在首轮完成 32 次 HTTP 200 后停止：
24 条合法响应保存，8 条校验失败触发错误记录代码中的属性错误，原始响应未成功落盘；第二轮未调用。
缺失响应不能重建、不能当成功，也不能从已经保存的成功子集推算两轮成绩。
原代码、prompt、请求、标签、日志和冻结摘要全部保留；追加停止说明及完整分母的未完成评估。

新执行契约只修复失败记录：使用异常的 `issue.code`，解析前立即保存原始响应，finally 持久化最终状态。
不修改候选 proof contract、system/pair/rubric、原 .12 critic 对照、输入、主输出、参照、生成参数或筛选门槛。
新根的完整 `requests_frozen.json` 必须逐字节等于原根。所有 64 个逻辑请求重新调用，不复用原 24 条成功响应。
本次对照仍是 19 条、两臂、两轮，各臂有 16 条需调用；3 条确定性绕过。
按原协议完成限定小试后即报告，不启动 96 条完整材料集、1,000 条或 20,000 条。

此次已经观察到候选有保护样本回退，修复执行后也不会就此放宽门槛或调整参照。
保存后的合法/非法输出一并报告；旧运行的 32 次调用另计，不因重跑而抹掉。
