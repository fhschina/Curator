# Source-character-order trial on unchanged v4 semantics

The completed explanation-first trial has 354 successful HTTP responses after one
bounded 429 retry. All 48 cross-arm error IDs were reviewed. The candidate has five
proof failures and worse all-row weighted primary accuracy in both repeats; reject
that ordering factor rather than layering repairs onto it. V4 remains the semantic
and output-order anchor, not a promoted full-development version.

H0606 repeatedly calls the unique entity a header even though its original position
is inside a liability-exemption sentence. H0641's old control repeatedly misplaces
the shared Italian negation because all shared spans precede all unique spans.
Test source order directly: list the same exact source spans, with existing IDs and
character ranges, separately in A and B original visible character order. Shared
spans still appear on both sides; no new paragraphs, summaries or outside context.
Do not change the v4 system or rubric, output fields, schema/property order, adapter,
materiality rule, main .12 outputs, model, generation or references.

This differs from old .17 full-text-plus-index and .18 added-context experiments:
it uses the current v4 independent-X/Y semantics and rearranges the same selected
source spans rather than duplicating whole source content or adding context windows.
Those earlier negative results remain preserved; no benefit from this factor is assumed.

Same 96 cases, all weights, two repeats. Fresh original .12 critic, exact v4 critic
with original grouped presentation, and source-ordered v6. Three targets and nineteen
guards unchanged. Actual control request hashes must match the original v4 trial.
All 354 requests use bounded transport-v2; no cache/output reuse or schema retries.
The latest reviewed run is the completed order/transport-v2 trial, even though the
semantic-control request anchor is the older v4 trial. Both are transitively frozen.

All errors need review before another iteration. No automatic full-development or
release promotion. Deadline stays 12:00:52 UTC; stop admitting new calls 210 s early.
