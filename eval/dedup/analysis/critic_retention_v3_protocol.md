# 新窗口第 2 轮：selected proof v3

前置：retention-v2 两轮全部完成并复核，96 次调用、候选每轮 13/16 输出无效，不扩大。
复核清单：`/raid/hfang/ihb/runs/critic-five-hour-20260911T070052Z/retention-v2-pilot/review_complete.json`。
本轮是新合同、独立运行根，不修改 v2 源码、结果、参照或失败状态。

沿用 [五小时协议](critic_five_hour_v1.md) 的截止、模型、主输出、输入、参照、完整分母和样本门槛。
原 .12 critic、retention-v2 编码对照、selected-v3 候选三臂各两轮，19 条，96 次逻辑调用。
不移除争议案例或修改强制保护。H0701 的新边界疑问单列等待用户，其原冻结 negative guard 在本轮仍记录。
没有达到完整 1,000 条 P/R 门槛时，局部 P/R 即使均过 75% 也不终止整个目标为成功。

唯一修复范围为证明编码：

- 保留 v2 的 X＋独立 Y、完整政策、真实责任主体冲突、翻译/界面和不确定性语义。
- source loss 从布尔值＋生成引文改为一个原文片段 ID 或空串。
- 每侧独立、必填一个 context ID；不是存在 loss 才需要对侧证据。
- actual conflict 独立于“额外内容”的 ID：从明确 conflict 和双侧主体证据推导双向否决，不让模型再填冗余布尔值。
- 复用已有严格 span selection 校验：验证全部原文片段及位置，不截取或猜测引文，不容许错侧或凭空 ID。
- 模型输出具体实例，不再在提示结尾放一个容易被照抄的完整 JSON Schema；schema 仍在本地严格校验。

H0347 的导航实质性误读尚未额外改规则；H0701 的完整性争议不靠改标签修分。
因此输出完成率改善不等于这些语义问题已解决。每轮继续阅读全部错误理由，再确定是否做单项语义修复。
若通过原局部门槛，须完成绑定结果摘要的逐错 review 后才可另建 96 条面板或完整评估。
