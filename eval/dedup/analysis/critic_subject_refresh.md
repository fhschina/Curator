# Fresh upstream stability check

After all errors of the full conditional proof-verifier trial are reviewed, retain
the same original 1,000 cases, labels, weights, .12 main outputs and saved coverage-v4
outputs. Repeat every one of the original 129 admitted subject-v1 requests with
identical bodies, no saved subject responses and no case-ID-based admission.

The final action contract is frozen before these subject calls: scope-v2 narrows
the original findings, and proof-verifier-v4 checks every proposed in-scope veto
twice with fresh responses. All other cases deterministically keep exact coverage.
The intermediate broad-subject score is diagnostic only, never the pipeline result.
A missing or malformed subject receipt blocks final composition; it cannot count
as a safe bypass. Verifier failures remain unresolved in the full denominator.

This is one predeclared stability iteration, not a fresh main/coverage rerun and not
independent generalization evidence. The existing 32-case protections and repeat
checks remain unchanged. Any new error or lost identity repair requires review;
no prompt, label or gate is altered after seeing fresh subject proposals.

Compare the entire population against fixed coverage, unverified scope and the
previous full verified composition. Review every observed error after completion.
Keep original and policy-v2 partial-draft scores separately; no release promotion.
No admission after 2026-09-11 11:57:22 UTC; hard deadline 12:00:52 UTC.
