# Context-budget-gated presentation, not a completed score

The unconditional intact-context full preparation failed before creating a manifest
or making any upstream call. On all129 eligible cases, H0837 needs28891 input tokens;
the frozen6144 reserve would exceed32768. Its original span presentation needs17703.
The failure is an engineering admission result, not a model error or missing label.

This candidate freezes a deterministic presentation gate before responses:
use full intact A/B plus original locator inventory only when input_tokens+6144
fits32768; otherwise use the EXACT original request body. Never truncate source,
increase budget, omit a case, change actions or select a route from predictions.
All original1000 rows/weights remain; all129 subject admissions remain. The same
fixed-proof verifier is required after any fresh subject run. Current data yields
128 intact presentations and one original-format fallback, selected by length only.

This preparation and its real-data tests do not constitute an online evaluation.
Do not report a score without every required subject and verifier output. Preserve
the failed unconditional preparation and all earlier completed results. No automatic
launch: the 12:00:52 UTC hard deadline and 11:57:22 admission cutoff still apply.
