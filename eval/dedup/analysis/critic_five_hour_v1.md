# .12 critic：新五小时迭代窗口

用户目标于 2026-09-11 07:00:52 UTC 重新建立。硬截止 2026-09-11 12:00:52 UTC。
此前目标累计时长、版本和调用保留，但不计入这个新窗口。截止后不发起新的在线请求。
达到原始 1,000 条开发集加权 P/R 同时 ≥75%，或到达截止，停止迭代并报告最好已验证结果。
主决策 ≥79%、证据/schema 完成率和保护样本仍单独检查；开发门槛通过不等于独立 holdout 或 release。
不启动 20,000、不查看 holdout、不 commit/push。复用原 key、同 27B 模型、温度 0，不升级模型。

固定 .12 原始主响应及主路由；保留旧参照和 policy-v2 部分草案，两套同时计分。
草案是已有规则的 AI 应用，不是独立 gold。本窗口不自行改标或删除争议样本。
主 NO/U 不重开；候选仅能撤销主 YES 方向。确切相同输入的绕过收益与 critic 语义收益分开。

## 第一轮：元数据与源侧保留证明分离

先离线诊断旧 scope-v1 的固定版本号错误；只做反事实定位，不把修改后的旧回答报作有效新回答。
新请求采用三臂、19 条原小试、各两轮：原 .12 critic、scope-v1 去除模型版本字段、retention-v2。
后两者的固定契约元数据由请求清单绑定，模型不能填写/覆盖。旧 schema 不放松。
retention-v2 从“拒绝哪个替代方向”改为“A/B 各自是否有未被对侧保留的内容”，方向由代码推导。
同步强调 shared 双侧保留和实际政策义务主体冲突。内容/payload、主输出、模型及预算保持一致。
这是有格式对照的 critic 组件实验，不将多项语义/表示改动称为纯单条规则消融。

所有 19 条始终计分。两轮候选的保护要求：H0537、H0514、H0850、H0316、H0701、H0760 保持 NO；
H0108、H0126、H0419、H0998 政策方向符合现有草案；H0007、H0893、H0347、H0417、H0810 等价正确。
H0093 的完整数据处理同意＋商品、H0195 的弱角色推断、H0352 的短信贷政策、H0748 的 Stories 片段，
作为预先声明的待人工裁决项：不改标签、仍全量计分，但不强迫候选遵循尚有争议的保护断言。
候选所有输出必须严格有效；保护正确、两轮稳定且主决策不低于对照，才考虑扩到保留全部 64 条成员的 96 条材料集。
96 条检查仍包含失败和争议；复核全部误判后才能启动完整 1,000 条 critic 重评，不能拼接局部预测冒充全量新成绩。

每次冻结后不编辑该版本源文件、请求或标签。新修复另建合同与运行根；运行先保存 raw 再校验。
每轮记录所有不一致：明确模型错误、协议/证据工程错误、口径/参照争议、仍待判断（含输入不足）。
明确错误也是 AI 开发判断，不冒称人工确认。争议逐条保存原文和理由供用户复核。
复核决定下一次最小有价值改动；不自动以版本号递增启动另一轮。

并发最多 2，间隔至少 2 秒；每逻辑请求传输尝试最多 2 次，语义/schema 自动纠错为 0。
每个在线任务在截止前 210 秒停止接收新调用，给当前调用的有界超时和响应落盘留余量。
预检/局部失败不是整个五小时任务成功；有可验证修复路径且未到截止时，继续下一次独立实验。
