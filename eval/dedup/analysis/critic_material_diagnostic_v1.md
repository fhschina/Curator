# Material diagnostic v1 — explicit scope revision, not a passed pilot

The strict-schema 19-case pilot passed local validation 32/32 but did NOT pass its frozen expansion gate:
H0701 remains an old-reference negative guard failure; H0760 varies across repeats. Preserve that assessment verbatim.
Every observed mismatch is reviewed and the user has been asked to arbitrate the concrete pending policy boundaries.
No labels, weights, guard results or old scores are changed. We do not tune the prompt to force pending old answers.

The user's five-hour cause-led iteration authorizes a separate diagnostic on the previously frozen material inventory:
all original 64 cases, all 39 old interventions, all 19 current pilot cases, 96 cases total. No new cherry-picked subset.
This is a purpose/range revision, NOT satisfaction of the earlier pilot gate and NOT a semantic prompt iteration.
Its manifest must say diagnostic96, predecessor_gate_passed:false, and retain the earlier instability.

Use the exact same fixed .12 main, original .12 critic control and v3 constrained candidate. Same payloads, model,
temperature, budget, transport pacing and local validator. Existing 19 control/candidate request hashes must match.
Two fresh repeats per arm; bypass unchanged main negatives/unresolved and complete identical input transparently.
Include all 96 rows in scores, including failures and pending references. Existing old negative guards stay reported.
No 75% pass or release can be emitted from diagnostic96 even if local metrics happen to exceed 75%.

Review EVERY new mismatch by actual original source and raw proof; distinguish fixed-main scope limits, engineering,
clear semantic errors, pending policy and incomplete input. Only then select a concrete next repair or test protocol.
No automatic full-development run, no holdout, no 20,000, no reference relabel and no commit/push. Deadline unchanged.
