# Named-target scope diagnostic

The full v4 plus scope-v2 run repaired H0606/H0809, but wrongly vetoed
H0669 (ordinary comment commands) and H0820 (generic administrator plurality).
All 162 cross-comparator error IDs are reviewed before this diagnostic.

Freeze the same original .12 main and the newly saved full v4 coverage result.
The only component change is an explicit per-side target-kind judgment and
restriction to two NAMED_ACTUAL_TARGET witnesses. Existing allowable binding
categories remain LIABILITY_PARTY, POLICY_SERVICE and FAILED_OBJECT. Evidence
validation runs before filtering. No general coverage, access/record scope,
reference, model, temperature or transport change is bundled in.

The new 32-case panel retains all 17 previously routed specialist pilot cases
and adds the newly observed mechanisms with identity, generic role, translation,
missing-field, UI and metadata protections. Old panels remain frozen.
Two fresh arms (old scope-v2 specialist and named-target candidate), two repeats,
all identical v4 bases within and across repeats. Neither main nor coverage is
called again. Both original and partial-draft labels remain in every score.

Admission requires H0606/H0809 repaired to NO/NO in both repeats, and all other
panel cases preserve their exact fixed coverage primary decisions, including
H0221 whose reference remains disputed. This prevents an erroneous named-shop
versus missing-name veto being rewarded merely because its old reference is NO.
All outputs must be valid and both candidate repeats agree. All observed errors
are reviewed before expansion. A passed local diagnostic is not a 75% goal pass.

Only after admission and review: freeze a fresh original full-1000 diagnostic
with both specialist arms, same fixed coverage for all 1,000, and no reuse of local
specialist outputs. Only actual bilateral-positive cases receive calls; other
rows remain explicit bypasses, not omissions. No release/holdout/20,000 admission.
Stop new calls by 2026-09-11 11:57:22 UTC; hard deadline is 12:00:52 UTC.
