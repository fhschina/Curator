# Full original 1000-case fixed-pipeline diagnostic

Admission requires the completely reviewed 96-case subject-scope replay: zero
new errors, zero clear target/guard failures, zero engineering failures and zero
repeat differences. This is diagnostic permission, not release qualification.

Freeze all original 1000 texts, weights and two reference layers unchanged. Reuse
only .12 original raw main outputs with v6-route. Run 250 applicable cases through
fresh original .12 critic and exact v4 coverage critic (500 calls). Original main
negative/unresolved and complete-exact cases bypass both. No local online critic
responses are reused in this full evaluation.

Before any calls, also freeze potential subject-v1 requests for every main-positive
complete bilateral-unique case. Once fresh v4 outputs are available, admit only
those still positive under the unchanged deterministic subject route. Both broad
diagnostic output and the predeclared v2 restricted action are saved. Final veto
authority is only LIABILITY_PARTY, POLICY_SERVICE, FAILED_OBJECT; other types are
diagnostic. No prompt/action changes based on the full results.

Original model/generation, strict supported schema projection and paced resilient
transport are unchanged. No semantic/schema retries; two bounded transport attempts.
Deadline 2026-09-11 12:00:52 UTC, new admission stops 210 seconds earlier. Any failed
component propagates to final unresolved engineering status and remains in all
1000 scoring denominators. Do not treat upstream coverage failure as safe bypass.

Publish full weighted/unweighted precision, recall, primary agreement and error
counts for saved .12 main/final, fresh legacy, fresh v4, and final narrow pipeline.
Score historical and partial-policy-v2 labels separately; no taxonomy relabelling.
The stop target is weighted precision AND recall >=75% with zero missing outputs;
the independent holdout/release gate remains untested. Review every observed error
with source evidence before a next iteration; pending-reference cases stay pending.
