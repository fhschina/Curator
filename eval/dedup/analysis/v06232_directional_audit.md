# V0.6.2.32：方向证据修复通过预检，正式评估被限流门控停止

2026-09-11 UTC。**主判断与critic后最终判断均11/11正确、100%有效、零模型纠错；
但critic遇到6次HTTP429，冻结的纯HTTP200预检条件失败，未提交两轮50条正式面板。**
这次目标证据问题得到定点验证，不应把服务限流说成模型仍填错证据，也不能把小预检当作75%达标。

见[冻结设计](v06232_design.md)、[本地边界](v06232_boundary.json)、[离线反馈探针](v06232_feedback_probe.json)
与[机器可读完整审计](v06232_directional_assessment.json)。

## 修复内容与边界

新版本 `.32` 增加文档所属ID清单及正反方向的绑定示意：A-in-B source在A、counterpart在B，B-in-A镜像。
原payload未改变；ID清单在Jinja中直接从原证据生成，不读取标签或旧预测。system、pair、YAML同步。
新反馈列出错侧字段、实际ID、可引用的对侧ID与根证据要求，并通过原安全反馈编码完整进入outer重试。
它不会替模型修改ID、覆盖状态或根证据；合法V5输出的接受集和公开判决完全复用旧适配器。
V5 schema、public v3、独立实质Y containment、实际冲突与整对翻译判据不变。

`.31`保存的13条原始响应离线重放：10条合法响应的公开输出完全不变，3次错误仍被拒绝，
但现在有字段级方向反馈。未切换响应版本标签或改写旧结果，这不是新增在线能力数据。

## 本地验证

- 996项CPU测试通过、46跳过、2项GPU测试排除；Ruff及格式检查通过。
- 四个真实Ray→本地HTTP→NDD→writer→binder边界通过，最终全套24次本地HTTP。
  两个纠错测试先返回反向错侧证据，只在收到具体反馈后重新生成合法响应，验证真实outer重试而非直接修补结果。
- 最初native测试在调用模型前发现框架把Jinja的`!=`误按字符串format解析；改用等价的A_ONLY/B_ONLY比较，
  并增加运行框架模板校验回归后重新测试。此问题在冻结前修复，未发生外部模型调用。
- 正式50条全部通过主输入预算检查：最长22,179 tokens，加4096输出与2048余量为28,323，小于32,768。
  实际critic与重试也保持原预算检查，不截断或删除样本。

## 在线预检

| 阶段 | 覆盖条数 | 调用对数 | 最终有效 | 主决策正确 | 模型纠错对数 | HTTP |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| main | 11 | 11 | 11 | 11 | 0 | 11次200 |
| critic后最终 | 11 | 6 | 11 | 11 | 0 | 6次200、6次429 |
| 两轮正式50条 | — | 0 | — | — | — | 未提交 |

五条负例由本次主判断保留，未伪造critic调用；所有实际响应均在首次outer attempt满足严格schema和证据校验。
两阶段native/outer模型纠错均为0；429是服务请求重试，单独统计，不能混算为Judge纠错。
共23次外部HTTP：17次成功、6次429；保存17条assistant消息，全部严格schema有效，逐stage/attempt计数无缺口。
完整主/最终11条均严格重放输入、实际`.32`prompt、原响应和公开输出；未声称重建完整HTTP请求体hash。

本次两个名单方向首次输出的证据均正确：

| 案例 | hard_conflict A / B | A-in-B source / counterpart | B-in-A source / counterpart |
| --- | --- | --- | --- |
| A短、B长（ab） | S001 / B001 | S001 / B001 | B001 / S001 |
| A长、B短（ba，上次终态） | A001 / S001 | A001 / S001 | S001 / A001 |

两者都保持真实双侧UNCOVERED与no/no。H0653仍正确处理为核心互译加额外Y的containment；
旧8条保护样本也保持正确。这里证明的是本次定点预检通过，不是随机代表集、重复稳定性或总体precision/recall。

## 为什么没有继续50条

冻结预检沿用先前的联合条件：语义全正确、100%有效、零模型纠错，且每次调用只发生一次HTTP200。
前三项全部通过，只有最后一项被critic的6次HTTP429打断。原runner据此保存preflight_assessment与stopped，
未提交正式repeat_1/repeat_2；没有观察结果后删掉429、修改门槛或重启该run。

下一步应保持`.32`语义prompt固定，先处理请求速率和退避，而不是继续改Judge。
应在下次调用前单独冻结执行/限流协议，明确运输重试与模型纠错的门槛和费用计数；不能追溯改写本次预检为通过。
仍保留0终态错误、≤1%模型纠错、完整1000条weighted75/75/79以及独立holdout要求。
本轮没有足够数据发布critic净收益或正式版本排名，不release、不扩大1000/20,000。

## 追溯与资源

Run root `/raid/hfang/ihb/runs/v0.6.2.32-directional-diagnostic`；digest
`7a3657934db32e41469896afc1b4de952cd56cabdbf056f25cb9aa1b2507ffe7`，216个冻结source/reference资产。
`.27`—`.31`冻结、旧标签/权重/缓存均未修改。本次仍通过显式env-file沿用旧Key，不保存值或摘要。
独占CPU Ray的launcher为PID862858；结束仅停止本次进程，不用全局ray stop。未commit/push。
