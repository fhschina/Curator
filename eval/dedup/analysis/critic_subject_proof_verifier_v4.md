# Fixed-proposal verification

Named-target reselection is rejected after all 17 pilot error IDs were reviewed.
It loses the actual liability exemption twice by substituting a different generic
author/site clause. Verifier v4 therefore cannot reselect subjects or propose a
new veto: it verifies the exact two subjects and predicate locators already chosen
by the saved, validated original subject-v1 stage and permitted by scope-v2.

All original text is shown intact, followed by the exact selected subject and
predicate quotations. The original explanation, final predictions and references
are hidden. The proposal is explicitly a hypothesis, not trusted factual guidance.
The four-field answer only classifies the selected targets and supports/rejects
that specific comparison. Unsupported or uncertain comparisons preserve the fixed
coverage result. Bad source proofs or inconsistent answers are engineering failures.

Pilot: same 32 cases, two repeats, all actual saved scope-v2 vetoes eligible (four
per repeat), other cases explicit deterministic bypasses. Both identity repairs
H0606/H0809 must remain; all other pilot cases retain the fixed coverage directions.
No reference correction, new main/coverage/subject calls or broad action authority.

Only after all pilot errors reviewed and zero protection/repeat/engineering failures:
freeze all original 1,000 cases and run both verifier repeats with new responses.
This is a full-population conditional scope evaluation over fixed upstream outputs,
not a fresh end-to-end upstream run or independent generalization test. Every
eligible proposed veto is verified; no subset output is spliced into a new score.
Both historical and partial-draft labels remain in every full denominator.

At 11:57:22 UTC stop new admissions; hard deadline 2026-09-11 12:00:52 UTC.
No holdout, 20,000-pair run, release, commit/push or model change is authorized.
