# .12 critic scope v1：局部对照结果

结论：已实现基于 v0.6.2.12 的窄职责 critic，并完成冻结的 19 条、两臂、两轮小试。
候选未通过准入，状态为 `STOP_BEFORE_EXPANSION`。不替换 .12，不注册正式 release，不扩大到 96、1,000 或 20,000 条。
本报告中的参照包含历史标签及部分 AI 政策应用草案，不是新的独立人工 gold。

## 实现边界

- 完全复用 .12 保存的主 Judge 响应及 `v6-route` 主决策；主模型在线调用为 0。
- 主 NO/U 不重开；完整相同原文不调用 critic。这两个路由在新调用的两个实验臂中相同。
- 候选只能移除主 Judge 的 YES 方向，不能增加 YES 或反转单向方向。
- 内容丢失必须指向将被丢弃的一侧；实际身份、义务、状态、角色或成员冲突仍可否决双向替代。
- 接受 `X+独立Y` 及完整相同政策＋商品；不再以“不是同一条记录的扩展”为否决理由。
- 新 critic 使用独立的结构化证明契约；不把旧 critic 的两个评分字段伪装成新证明。
- 原 39 条介入记录全部导出，原 64 条面板成员全部保留在 96 条材料集合中。本次只调用其中冻结的 19 条小试。

实现见 `judging/critic_intervention.py`，配套 system / pair prompt / YAML 为
`resources/local_ndd/v06212_critic_scope_v1_*`。冻结协议见 [scope v1 protocol](critic_scope_v1_protocol.md)。
当前可靠执行入口为 `python -m eval.dedup.analysis.critic_scope_execution`；
不要使用已冻结的 `critic_scope_experiment.run`，其首轮失败记录路径有已知缺陷。
后者的准备、严格重放和评估函数仍由新执行器复用。

这是主 Judge 固定、critic 组件改变的对照；critic 的职责表达和内部证明格式同时变化，
不是只改一句规则的纯语义消融，不能把所有退化都归因于“收窄介入范围”。

## 执行与完整性

两臂同模型 `nvidia/qwen/qwen3.8-27b`，temperature 0、top-p 1、输出上限 4096、thinking 关闭。
两臂看到同一原始 span 证据包，不传主预测、标签、review ID 或抽样理由。
每臂每轮 16 次调用，另有 3 条确定性绕过：H0316 主 NO；H0417、H0810 完整相同原文。
不是 19 次调用中挑出 16 次成功结果。

完成目录：`/raid/hfang/ihb/runs/v06212-critic-scope-v1-fixed-execution`。

| 项目 | .12 critic 对照 | scope v1 候选 |
| --- | ---: | ---: |
| 两轮逻辑调用 / HTTP 200 | 32 / 32 | 32 / 32 |
| JSON 可解析 | 32 / 32 | 32 / 32 |
| 严格契约与证据有效 | 32 / 32 | 16 / 32 |
| 输出失败 | 0 | 16（每轮相同 8 条） |
| 传输重试 / schema correction | 0 / 0 | 0 / 0 |
| 每轮完整分母 | 19 | 19 |

所有 64 次调用都有请求、校验前原始响应及最终收据。无效回答没有伪造为有效输出，
评估时使用仅供计分的 U 占位，保留全部 19 条分母。
两轮候选最终主决策/失败位置一致，不等于稳定正确；对照在 H0195 上一轮 YES、一轮 NO。

另计首轮中止运行 `/raid/hfang/ihb/runs/v06212-critic-scope-v1`：
32 次 HTTP 200，24 条响应已保存；8 条候选校验失败后，执行器错误地读取异常的 `.code`，
导致这些原始回答未落盘。已如实记录为不可恢复，第二轮未调用，不能以成功子集报告完整成绩。
修复改用 `.issue.code`，且先保存原始响应再校验；未修改语义 prompt 或参照。
中间的 `v06212-critic-scope-v1-execution-v2` 准备目录因不可变清单写入冲突失败，模型调用为 0。
本轮所有目录合计 **96 次外部调用，96 次 HTTP 200，0 次传输重试**。

## 局部成绩：必须保留参照来源

以下全部是刻意富集难例的 19 条面板成绩，不能外推为完整开发集表现或用于宣布达到 75%。
两轮是相同样本的重复调用，不是 38 条独立样本。
P/R 分别为 duplicate precision / recall；主决策一致要求两个方向及同组判断均一致。
无效输出在主决策一致率中计为不正确；正例上的无效输出计入 recall 缺口。

### 部分 policy-v2 草案参照

| 实验臂 / 轮次 | 非加权 P | 非加权 R | 主决策一致 | 加权 P | 加权 R | 加权主决策一致 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 原 .12 critic / 1 | 85.71% | 60.00% | 13/19（68.42%） | 79.10% | 46.70% | 64.27% |
| 原 .12 critic / 2 | 100.00% | 60.00% | 14/19（73.68%） | 100.00% | 46.70% | 70.06% |
| scope v1 / 1 | 75.00% | 60.00% | 9/19（47.37%） | 67.96% | 48.51% | 40.12% |
| scope v1 / 2 | 75.00% | 60.00% | 9/19（47.37%） | 67.96% | 48.51% | 40.12% |

### 原历史参照（不覆盖）

| 实验臂 / 轮次 | 非加权 P | 非加权 R | 主决策一致 | 加权 P | 加权 R | 加权主决策一致 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 原 .12 critic / 1 | 85.71% | 85.71% | 16/19（84.21%） | 79.10% | 74.15% | 81.64% |
| 原 .12 critic / 2 | 100.00% | 85.71% | 17/19（89.47%） | 100.00% | 74.15% | 87.43% |
| scope v1 / 1 | 62.50% | 71.43% | 8/19（42.11%） | 50.67% | 57.44% | 34.33% |
| scope v1 / 2 | 62.50% | 71.43% | 8/19（42.11%） | 50.67% | 57.44% | 34.33% |

历史保存 final 在该面板按部分草案为 12/19 一致；只修相同原文的绕过路由变为 14/19。
这两条恢复是 H0417、H0810，属于确定性路由收益，不是新 critic 的语义收益。
保存结果、新调用 .12 对照、候选结果均分开保留；不能把服务重跑变化算作候选改进。
也未将 19 条候选结果拼回旧 1,000 条输出制造新的总体分数。

## 逐类归因

每轮候选 19 条分为：9 条按部分草案正确，2 条参照下的 over-group，8 条契约/证明失败。

| 案例 | 观测 | 归因与边界 |
| --- | --- | --- |
| H0126、H0419、H0998 | 契约名输出成 `dedup-v0621-critic-intervention-v1`，漏了一个 `2` | 3 条纯元数据错误。仅在内存副本中替换该常量后，其余证据校验通过，方向符合草案；这是未计分的定位检查，原回答仍失败，不做自动修复后重算。 |
| H0093 | 行动声明拒绝 A 替代 B，解释却最后支持 A 保留 B、拒绝反向；两侧都只引用 shared | 方向/证明不一致；同时其旧 NO 标签有政策边界问题，见下文。不能当作纯格式错误。 |
| H0352、H0701、H0760 | 把 S### 的政策或状态内容说成 B 未保留，用 shared 引文证明 A-only 丢失 | 实质证据理解错误被校验拦下，不只是引用格式不漂亮。共享内容实际在双侧。 |
| H0748 | 将 B001 的 Stories 内容解释为 A 的新增内容，并否决相反方向 | 文档侧别/方向错误；该片段是否实质内容另有参照争议，两件事分开。 |
| H0850 | KEEP_MAIN：把两个不同论坛的实际服务主体当成模板可变槽 | 明确语义回退。对照两轮均 NO；候选两轮均 YES。不能因条款复用而忽略义务所属主体。 |
| H0195 | KEEP_MAIN：把产品相关栏目与月份归档相关栏目解释为普通 UI | 冻结参照下 over-group；角色推断证据较弱，对照也有一轮不稳定，需要独立复核，不冒充已确认 gold。 |

候选每轮四条政策方向目标仅 H0108 通过，其余三条被常量拼写错误阻断。
八条冻结负例保护通过 3 条：H0316、H0514、H0537；其余是 3 条证明失败和 2 条 YES，
不能把这五条统称为五条“已证实语义误合并”。
五条等价保护全部通过，其中两条为相同原文绕过，另外三条为两条翻译及一条 chrome/同意按钮案例。
新候选修好 H0108，但放过 H0850；尚无整体净收益证据。

### 新发现的参照边界：H0093

全文显示 B 为 1,396 字符的具名个人数据处理同意文本，包含主体、目的、数据类别、处理动作、方式及期限；
A 完整保留 B，另增加 PMP500 商品技术参数。不能只因第一句提到点击提交，就直接断言整个 X 是空 UI。
这与已接受的“完整相同政策＋商品”口径存在需要复核的交叉，而旧 AI 复核把它列为明确负例。
本次冻结参照、保护名单及得分均不回改；记录该冲突供下一次独立政策应用复核。
即使其参照以后修订，本次方向证据仍无效，H0850 的身份冲突回退也不会因此消失。
H0352 的短政策完整性、H0748 的 Stories 片段争议同样未被强行裁决。

## 下一步边界

本轮到此停止在线扩展，不继续在同一个已观察面板上改 prompt 然后覆盖重跑。
若启动后续候选，先冻结独立修改记录：

1. 将固定契约元数据与模型要判断的语义分开，避免让模型抄写长版本号成为新的失败源；不得事后宽松接收本轮错版本输出。
2. 单独验证 shared 内容双侧保留、A/B 侧别及替代方向，先使用镜像合成样本保护，再动实际案例。
3. 保留 .12 对真实服务主体冲突的保护；针对 H0537/H0850 的不一致理解，明确“新增独立主体”与“同一义务槽换主体”的区别。
4. 独立复核 H0093 等参照交叉项，保持旧标签和历史得分。若确认改标，所有历史版本使用同一新版参照重算。

不能据此判断 75% 门槛过高，也不能宣称 scope v1 已改善总体 precision/recall。

## 校验与可复核产物

- CPU 测试：`1137 passed, 50 skipped, 2 deselected`，使用现有环境，范围 `tests/eval/dedup`，排除 GPU。
- 新增一对一测试：critic 23 项、实验冻结/评估 3 项、真实本地 HTTP 执行边界 5 项，共 31 项。
- 新增六个 Python 文件的 Ruff 检查及格式检查通过；`git diff --check` 通过。
- 原参照冻结、首轮冻结、修复执行冻结均核验通过；完成后离线重放 `assessment.json` 与完成收据的摘要一致。
- 请求文件与首轮逐字节一致：`1d37c0e6c3f5c6bc8015e68111e20dea4d0f0c5131c7d12ee8522765e8f78216`。
- 新执行 contract digest：`054eb5e8112e5ab5163d12c8eebdf353f248994250178bccf2268c371d84f7a9`。
- 结果摘要：`f313eb47ebd579cd1cca0f58f82c974c85d86d31ebdb280a1001e6ffa118eb62`。

完成目录内：`assessment.json`、`complete.json`、`requests_frozen.json`、`panel_private.json`、
`responses/`、`transport_events.jsonl`。原文和响应为私有复核材料，不嵌入公开样例。
`base_freeze/interventions39_private.json` 保存全部 39 条旧介入；`base_freeze/review/` 保存匿名输入与私有映射。
未覆盖旧 judge cache、版本配置、原始输出、参照或既有冻结记录；未 commit/push。
